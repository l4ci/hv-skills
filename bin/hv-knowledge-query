#!/usr/bin/env bash
# Print the matching ## Topic section(s) from .hv/KNOWLEDGE.md.
# Lookup helper (thin wrapper): always exits 0; missing topics are silent.
# Usage: hv-knowledge-query <topic>...
exec "$(dirname "$0")/hv-section-query" knowledge "$@"
