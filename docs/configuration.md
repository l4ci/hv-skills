# Configuration

All settings live in `.hv/config.json`. Use `/hv-config` to edit interactively, or hand-edit the file.

## models — orchestrator and worker model selection

## work — isolation and merge strategy

## refactor — confirm before execute

## learn — verification pass

## ship — review before integration

## autonomy — `off`, `auto`, `loop`

## debug — competing hypotheses
