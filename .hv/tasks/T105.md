# T105 — Close pre-existing drift in the hvlib exports sentinel

`test/sections/44_hvlib_exports.sh` pins the `from hvlib import X` surface, with a maintenance rule that every name helpers import that way must appear in its `NAMES` list. Refactor round 6 (commit `bf49f02`) added the two names inside its own mandate (`COUNTABLE_TYPES`, `locked`) but verified that ~13 names helpers already import were never registered:

- `bump_hit`
- `load_sidecar`
- `load_tier_sidecar`
- `save_tier_sidecar`
- `managed_block_regex`
- `compute_managed_block_inputs`
- `resolve_cycle_ids`
- `resolve_knowledge_target`
- `resolve_tier_sidecar`
- `section_name_for_id`
- `set_todo_field`
- `iter_open_bullets`
- `BACKLOG_FILE`

## Acceptance

- Re-grep `bin/` for `from hvlib import` and reconcile the full NAMES list against actual helper imports (the list above is the round-6 snapshot; verify before adding).
- Sentinel still passes (`bash test/sections/44_hvlib_exports.sh` standalone or via the runner).
- Optional hardening: a comment or check that explains how to regenerate the expected list, so the drift class can't silently reopen.
