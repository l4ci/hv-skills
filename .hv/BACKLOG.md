# TODO

## Bugs

## Features
- **[F05] [Major] `/hv-freeze` scope lock for parallel orchestrators.** Per-stream file-edit boundary enforced via PreToolUse hook. `/hv-work` writes declared scope to `.hv/locks/<stream>.json`; hook rejects writes outside it. Auto-released on completion/pause. Distinct from worktree isolation — worktrees protect filesystem between streams; freeze protects intent within a stream. Most valuable for multi-stream `/hv-work` and `/hv-refactor`. Detail: `.hv/features/F05.md` Since: a0e7f1f

## Tasks

## Completed
- ~~**[B01] [P1] `/hv-capture` Step 7 auto-dispatches `/hv-brainstorm` on `autonomy=auto`, conflating capture with planning.** Capture should be pure intake; auto-invoking brainstorm pulls the user into design exploration mid-brain-dump. Worse, the escalation is inverted: `auto` proactively brainstorms, `loop` skips entirely. Fix: Step 7 prints the nudge line in all modes (off/auto/loop) and never invokes the Skill tool. Move autonomous advancement to `/hv-next` where "advance without asking" semantics belong. Related: [F01], [F03], [F05]~~ Done 2026-05-14 [`0128cbd`]
- ~~**[B02] [P2] `hv-knowledge-merge` creates duplicate topic headers on slight name variation.** Helper dedups by `(topic, title)` only — two semantically identical topics with different wording (e.g. `Build & Tooling` vs `Build & Tooling: Helpers`) produce separate `##` headers, sometimes empty after dedup. Observed in Fps (`Build & Tooling (1)`, `Godot 4 (1)` both empty). Fix: fuzzy topic match before creating a new section, or surface the closest existing match before insert.~~ Done 2026-05-15 [`e4abdbe`]
- ~~**[B03] [P2] `hv-skills-index` injected CLAUDE.md block omits `/hv-issues`, `/hv-map`, `/hv-ship --undo`.** The hardcoded heredoc in `bin/hv-skills-index` lists 24 skills across 6 categories (Capture & pick / Plan & build / Review & ship / Persist / Vision & docs / Maintenance) but misses three real user-facing skills. Result: projects running `/hv-init` don't surface these in their managed block, hurting discoverability — `/hv-ship --undo` in particular is a recovery skill users won't find when they need it. Fix: add the three to the hardcoded body (decide category per skill) AND add a smoke test that asserts every `hv-*/SKILL.md` folder appears in the index body, so future skill additions can't drift again. Related: [B01]~~ Done 2026-05-15 [`dbda7b4`]
- ~~**[F02] [Minor] Iron Law in `/hv-debug` — hard stop after 3 failed fixes.** Adopt gstack `/investigate` rule: no fix attempts without a stated root-cause hypothesis; hard stop and escalate after 3 failed attempts. Prevents the random-try failure mode that burns sessions. Counter in `.hv/debug/<session>.json`, fail-loud summary on 3rd failure.~~ Done 2026-05-15 [`401253f`]
- ~~**[F04] [Minor] Second-opinion gate before `/hv-ship`.** Spawn a fresh subagent with no prior conversation context, give it only the diff + the goal, ask it to find what's wrong. Same-model-fresh-context is the cheap MVP; cross-model (Codex/Gemini) is the gstack `/codex` gold standard. Output feeds `/hv-ship` pre-merge gate. Catches blind spots `/hv-review` shares with the work it produced. Related: [F01]~~ Done 2026-05-15 [`181844f`]
- ~~**[F06] [Minor] Skill-format CI validator (GitHub Actions).** Lint every `hv-*/SKILL.md` statically on PR: frontmatter, banner block, references resolve, `plugin.json` vs. `CHANGELOG.md` version match. Codifies `references/authoring-conventions.md` so authoring drift is caught at PR time rather than smoke-suite/install. Detail: `.hv/features/F06.md` Milestone: M05~~ Done 2026-05-15 [`0169181`]
- ~~**[T01] Avoid `&` in TodoWrite task titles — Claude Code TUI renders it as `&amp;`.** Upstream rendering bug in Claude Code (entity-encoded, never decoded); cannot fix here. hv-skills-side action: (a) note the workaround in authoring conventions, (b) sweep hv-*/SKILL.md and references for TaskCreate/TodoWrite payloads containing `&`, replace with 'and' or '+'. Detail: `.hv/tasks/T01.md` Related: [F06]~~ Done 2026-05-15 [`e6efa95`]
- ~~**[F08] [Minor] Silent-failure-hunter check in /hv-ship and /hv-review.** Integrate a verification sub-step into the existing skills that detects when tasks report 'complete' but didn't actually work — tests pass but feature broken, build succeeds but artifact missing, smoke test reports OK but the affected code path was never exercised. Not a new skill; lives as a shared reference invoked from both ship and review before they mark work done. Inspired by ECC's agents/silent-failure-hunter.md archetype.~~ Done 2026-05-15 [`8bc48f2`]
- ~~**[F07] [Minor] Two-stage `/hv-review` split (spec-compliance → code-quality).** Stage 1 diffs the change against `PLAN.md` outcomes; stage 2 (quality/style/edges) only runs when stage 1 passes. Short-circuits diffs that don't match intent and leverages the existing `/hv-plan` artifact. Detail: `.hv/features/F07.md` Related: [F04] Milestone: M05~~ Done 2026-05-15 [`ec66e18`]
- ~~**[F03] [Major] Knowledge promotion lifecycle in `/hv-learn`.** Tier KNOWLEDGE.md entries: `provisional` on first capture → `confirmed` after N successful applications without contradiction → `deprecated` on contradiction signal. Cuts drift; gives `hv-knowledge-query` consumers a confidence tier. Inspired by gstack domain-skills quarantine→active lifecycle. Detail: `.hv/features/F03.md` Related: [F01]~~ Done 2026-05-15 [`cc5f536`]
- ~~**[F09] [Minor] Manual `/hv-ship --docs` should check docs against recent changes.** Currently when docs/ exists and `docs.afterWork` is on, re-running `/hv-ship --docs` manually prints a no-op message and exits. Instead it should run the after-work flow (gather diff since last `docs:` commit, classify changes, map to pages, propose updates) — same path the post-cycle auto-trigger takes, but bypass the 2+ items / ≥5 files / hard-bug gate since manual invocation is itself the trigger. Surfaces drift without requiring a fresh `/hv-work` cycle.~~ Done 2026-05-15 [`8753559`]
- ~~**[B04] [P1] `hv-todo-field` rejects `title` though SKILL prose calls for it.** `/hv-brainstorm` Steps 2 and 3 invoke `hv-todo-field <ID> title` for existence + display; helper validation rejects with 'title is not a valid field; pick one of detail/related/milestone/repos/subsystem' and exits 1. The Python block already extracts the title internally as `_title` — fix is to add 'title' to the valid-fields set and `print(_title)` when requested. Surfaced while brainstorming F01. Related: [F01] Subsystem: brainstorm~~ Done 2026-05-15 [`d1c0572`]
- ~~**[F01] [Major] Browser-driven QA skill (`/hv-qa`).** Drive a real browser to verify running-app behavior — three sub-modes: `--perf` (Lighthouse/CWV), `--a11y` (axe-core), `--ux` (LLM-as-judge on screenshot+DOM against explicit rubric). Findings route through `/hv-capture` with severity gate. Inspired by gstack `/qa` + `/devex-review`. Detail: `.hv/features/F01.md` Related: [F03], [F04]~~ Done 2026-05-15 [`9c74419`]
- ~~**[B05] [P2] `hv-update-check` trusts `CLAUDE_PLUGIN_ROOT` without verifying plugin name.** Resolver accepts whatever plugin is the active Claude Code context (e.g. context-mode), so currentVersion/installRoot are reported as that plugin's. Production path narrow (slash-command invocations set CLAUDE_PLUGIN_ROOT to hv-skills correctly), but buggy code ships in .hv/bin/hvlib.py to every downstream project. Detail: `.hv/bugs/B05.md` Subsystem: bin~~ Done 2026-05-15 [`b521f7e`]
- ~~**[F11] [Minor] Remove /hv-capture alias entirely.** Cut hv-c/ skill folder, plugin.json + marketplace.json entries; users type /hv-capture instead. Smallest cut, zero semantic risk — should land first to validate the cut-and-clean pattern. Milestone: M01~~ Done 2026-05-16 [`cae95b6`]
- ~~**[F15] [Minor] Fold /hv-work --preview into /hv-work --preview.** Remove hv-assume/ skill; add --preview flag to /hv-work that prints intended files, tests, and assumptions without dispatching workers or making any writes. Same output shape /hv-work --preview produces today. Milestone: M01~~ Done 2026-05-16 [`4a1b3b3`]
- ~~**[F14] [Minor] Fold /hv-capture --remove into /hv-capture --remove <ID>.** Remove hv-rm/ skill; add --remove flag to /hv-capture that strips the item and cleans up its dependency edges in BACKLOG.md and status.json. Related: [F16] Milestone: M01~~ Done 2026-05-16 [`b946d48`]
- ~~**[F16] [Minor] Fold /hv-issues into /hv-capture --from-github / --from-gitlab.** Remove hv-issues/ skill; add the two pull-and-classify flags to /hv-capture, writing GH: #N / GL: #N cross-refs into the captured entries. Round-trip close on /hv-ship stays as-is. Related: [F14] Milestone: M01~~ Done 2026-05-16 [`2941c03`]
- ~~**[B06] [P2] `hv-todo-drift` false-positives on reused IDs across machine syncs.** Drift detector walks full `git log` and matches `[ID]` tags regardless of when the commit landed; old commits referencing IDs that since got reused trigger false drift on the new items. Just hit by 8 features (F12–F22) on this machine. Fix: anchor each open item to a capture-time commit hash via `Since: <hash>`; `hv-todo-drift` skips commits before that anchor; backfill helper stamps existing items. Detail: `.hv/bugs/B06.md` Subsystem: bin Milestone: M01~~ Done 2026-05-16 [`9aebe63`]
- ~~**[F13] [Minor] Fold /hv-ship --undo into /hv-ship --undo.** Remove hv-undo/ skill; port the last-merge rollback logic (git revert, restore TODO entries, reset .hv/status.json) into /hv-ship as a --undo flag. Sweep SKILL.md cross-refs in surviving skills. Related: [F17] Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`e7e4dd9`]
- ~~**[F17] [Minor] Fold /hv-ship --docs into /hv-ship --docs + auto-trigger when docs/ exists.** Remove hv-docs/ skill; move docs-sync logic into /hv-ship as a --docs flag, plus auto-trigger when the project has docs/ AND docs.autoCreate is true. Update config schema. Related: [F13] Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`e7e4dd9`]
- ~~**[T02] Sync `bin/hv-skills-index` body with the post-fold command surface.** The hardcoded heredoc still lists `/hv-capture (alias /hv-capture)`, `/hv-capture --remove`, `/hv-work --preview`, and (now-defunct) `/hv-issues` — the F11 (/hv-capture removal), F15 (/hv-work --preview fold), F14 (/hv-capture --remove fold), and F16 (/hv-issues fold) cycles updated CLAUDE.md directly but left the canonical regen script stale. Next `/hv-init` in a hv-skills-using project would regress those cleanups by re-rendering the old text. Fix: rewrite the heredoc body to match current command surface (`/hv-capture` with `--remove` and `--from-*` notes, no `/hv-capture` alias mention, `/hv-work --preview` instead of `/hv-work --preview`), and consider extending the [B03] smoke test (or adding a sibling) to assert no entry in the index body references a non-existent `hv-*/` folder. Also pre-empt the upcoming F13 (/hv-ship --undo fold) and F17 (/hv-ship --docs fold) by structuring the heredoc as a function of the actual skills/ directory rather than a hardcoded list. Related: [B03], [F13], [F17] Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`3cdf9e1`]
- ~~**[F12] [Minor] Convert /hv-map to auto-only; fold first-run scaffolding into /hv-init.** Remove hv-map/ skill folder and manifest entries; move the MAP.md template and .hv/map/ dir creation into /hv-init's setup. Keep the auto-bump that already fires from /hv-work and /hv-ship. Update CLAUDE.md managed-block seeding to call the new init path. Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`f3c8656`]
- ~~**[F18] [Major] Fold /hv-learn --term into /hv-learn --term <name>.** Highest-risk fold — sequence last. Remove hv-context/ skill, add --term flag to /hv-learn, define the CONTEXT.md → KNOWLEDGE.md data-migration story consumed by /hv-migrate v4. Detail: `.hv/features/F18.md` Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`f45355e`]
- ~~**[F19] [Major] /hv-migrate v4 codemod.** One-shot rewriter that scans .hv/ and project CLAUDE.md for the 8 removed command names, rewrites to new shape, backs up everything touched. --dry-run default, --apply to write, idempotent. The codemod is the only safety net for the hard cut. Detail: `.hv/features/F19.md` Related: [F18] Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`1e983fd`]
- ~~**[F22] [Major] /hv-init mirror-cleanup + /hv-migrate v4 explicit-removal pass.** /hv-init Step 2 currently uses cp "$SRC"/hv-* — overwrites matching names but never removes stale ones. After F18 ships, .hv/bin/hv-learn --term-add lingers alongside the new hv-glossary-write on every downstream project. Fix in two places: /hv-init mirrors canonical bin/ (rsync --delete or find -delete + cp); /hv-migrate v4 carries an explicit removed_binaries list for v4 cuts. Defense in depth. Detail: `.hv/features/F22.md` Related: [F18], [F19] Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`b77bdcd`]
- ~~**[F24] [Major] hv-self-locate preamble consolidation across ~80 helpers.** The 4-line `HERE=...; . hv-self-locate.sh; hv_self_locate` preamble is repeated across ~80 `bin/hv-*` helpers. Bootstrap-order changes (PYTHONPATH, error path, HV_ORIG_PWD) require touching 80+ files. Consolidate via: (a) source-once helper that internalizes HERE detection, (b) new `hv-preamble.sh` with the whole prelude, or (c) inline `. "$(dirname "$0")/hv-self-locate.sh"; hv_self_locate` (1-line caller). Needs proper /hv-brainstorm + /hv-plan; XL blast radius. Detail: TBD. Milestone: M01 Since: c84ae3e~~ Done 2026-05-16 [`dc2b49d`]
- ~~**[F20] [Major] v4.0 marketing artifact bundle: ANNOUNCEMENT + CHANGELOG + README rewrite.** Three coupled artifacts that carry the 'cut IS the marketing' story. Announcement drafted FIRST to lock the framing; CHANGELOG iterative as cuts land; README rewritten LAST. Lead with outcome, complexity-budget framing, every cut gets a 'so you can' clause, receipts table near the bottom. Detail: `.hv/features/F20.md` Milestone: M01 Since: a0e7f1f~~ Done 2026-05-16 [`fe51c22`]
- ~~**[F27] [Minor] /hv-capture should audit code state before capturing from stale milestone spec.** When `/hv-capture` is invoked against a milestone spec (`.hv/milestones/M<NN>.md`), it appends items without auditing whether they're already shipped under different IDs — if the milestone spec hasn't been kept in sync with code state, the capture creates duplicates and loop mode can then dispatch `/hv-work` against already-done items. Detail: `.hv/features/F27.md` GH: #11 Since: d6f3d20~~ Done 2026-05-17 [`214783b`]
- ~~**[B07] [P1] hv-migrate reads top-level `version`, /hv-init writes nested `hvSkills.version`.** Documented v3 → v4 upgrade dead-ends at "refusing: .hv/config.json has no 'version' field — run /hv-init" even after /hv-init has run. Workaround: hand-stamp top-level `version` via hv-config-set. Fix: nested-first read with legacy fallback. Detail: `.hv/bugs/B07.md` Related: [B08] Milestone: M01 Since: a613b50~~ Done 2026-05-17 [`3ab5eff`]
- ~~**[B08] [P1] /hv-migrate v4 --apply never bumps the recorded version stamp.** After successful apply, hvSkills.version still reports the pre-migration value; hv-preflight keeps nagging "project at X.Y.Z, plugin at A.B.C — run /hv-init" indefinitely. Functional idempotency holds via the plan-based nothing_to_do branch, but recorded state stays stale. Detail: `.hv/bugs/B08.md` Related: [B07] Milestone: M01 Since: a613b50~~ Done 2026-05-17 [`3ab5eff`]
- ~~**[B09] [P1] v3→v4 upgrade: hv-init leaves orphaned managed blocks; hv-migrate codemod is a blind regex (corrupts mentions + helper substrings).** Three defects: (1) /hv-init upgrade-refresh never strips orphaned v3 `<!-- hv-context-* -->` blocks; (2) hv-migrate rewrite_text() is line-regex with no mention/usage discrimination or managed-block awareness (2a corrupts self-documenting text, 2b substring-matches hv-map-query, 2c rewrites instead of deleting orphan block); (3) umbrella projects have no migration path (F21 deferral). Detail: `.hv/bugs/B09.md` Related: [B07], [B08], [F21] GH: #14 Milestone: M01 Since: 74ca9df~~ Done 2026-05-17 [`3ab5eff`]
- ~~**[F25] [Major] hvlib.py package split — 1077 LoC → 6 modules.** hvlib.py owns 8+ unrelated concerns: section-edit, bullet parsing, frontmatter, milestone/repo registry, atomic IO, managed-block, TOML version handling, map iteration, crossref. 14 PYTHONPATH heredoc callers each implicitly know their subset. Split into hvlib/{io,section,bullet,repos,version,crossref}.py with top-level `from hvlib.* import *` preserving backward compat. Needs proper /hv-brainstorm to validate the wildcard re-export strategy and verify every PYTHONPATH caller. Related: [F24]. Milestone: M01 Since: c84ae3e~~ Done 2026-05-18 [`bdaaecf`]
- ~~**[B10] [P2] `issues.providers.{github,gitlab}` config gate accepts None as enabled.** /hv-capture --from-github / --from-gitlab Step I1 reads `cfg.get('issues',{}).get('providers',{}).get('github')` and only stops when the value is literally `false`. On a project that never set the key (config has no `issues` section at all, returns None), the import proceeds silently. Either the key should default to `true` explicitly (current de-facto behavior, just make it schema-visible) or the gate should require `is True` to enable. /hv-init's EXPECTED schema check doesn't include `issues.providers.*` either, so STALE migration never prompts for it. Subsystem: hv-capture Milestone: M01 Since: 4f17322~~ Done 2026-05-18 [`1ba7897`]
- ~~**[F26] [Major] design/plan/spike/vision CRUD triplet consolidation.** Four near-isomorphic CRUD families (16 helpers total: hv-design-{add,rm,show,list}, hv-plan-*, hv-spike-*, hv-vision-* CRUD subset). Each `-rm` is find-then-unlink; each `-show` is find-then-cat; each `-list` walks dir + emits JSON. Adding a 5th artifact kind = 4 new helpers. Approach options: (a) lightweight `hv-artifact-lib.sh` shared by 16 wrappers, (b) full `hv-artifact <kind> <verb>` dispatcher with SKILL.md callsite sweep, (c) Python `Artifact` class in hvlib. Needs /hv-brainstorm. Out of Phase A refactor due to API-shift implications. Milestone: M01 Since: c84ae3e~~ Done 2026-05-18 [`1485be5`]
- ~~**[B11] [P2] hv-managed-block lost --strip-deprecated mode; smoke section 'hv-managed-block — B09: --strip-deprecated removes orphan v3 blocks' fails on main.** Error: `unknown key '--strip-deprecated' (known: knowledge, decisions)`. Likely regression from `c623d2f refactor: simplify hv-skills surface`. Smoke ran red on main before F26 work — F26 ships unaffected. /hv-init's deprecated-block stripping pipeline is broken until restored. Since: 51c5540~~ Done 2026-05-18 [`6922ab2`]
- ~~**[T03] hv-learn Step 8 auto-split orphans knowledge-tier.json (silent tier/hit loss).** `/hv-learn` Step 8 auto-split (steps 3–7) is silent on the tier sidecar — it specifies only KNOWLEDGE.md heading/bullet moves + `hv-knowledge-index`, but renaming a `## Topic` heading orphans every titled bullet's `.hv/knowledge-tier.json` entry, which is keyed `"<topic>::<title>"`. Detail: `.hv/tasks/T03.md` GH: #13 Since: d6f3d20~~ Done 2026-05-18 [`80fa6fd`]
- ~~**[T04] Orchestrator absorbs wave-internal file collisions for parallel dispatch.** When a multi-task plan has two T# steps that touch the same file and `work.isolation` is `branch`, the parallel-dispatch guard forces them to serialize unless the orchestrator merges one task's edit into the other at dispatch time. Detail: `.hv/tasks/T04.md` GH: #12 Since: d6f3d20~~ Done 2026-05-18 [`ff5f330`]
- ~~**[T102] No writer helper to set a backlog item's `Milestone:` (or other) field after capture.** `/hv-plan` Step 2 and `/hv-capture` Step 4.5 both require tagging an existing BACKLOG.md entry with a milestone, but the only field helper is the read-only `hv-todo-field`. Today this forces hand-editing `.hv/BACKLOG.md`, which the project explicitly forbids ('don't edit .hv/ by hand — use the skill helpers'). Add a `hv-todo-set-field <ID> <field> <value>` writer (mirroring `hv-todo-field`'s field vocabulary: milestone/related/repos/subsystem) and route `/hv-plan`'s tag step and any retag flow through it. Surfaced while planning M06-F21 (F21 had to be milestone-tagged by hand-editing). Milestone: M01 Since: e7dbbf0~~ Done 2026-05-19 [`47294c2`]
- ~~**[F21] [Major] Umbrella-aware KNOWLEDGE.md.** Per-sub-repo .hv/knowledge/<name>/KNOWLEDGE.md storage; rewire hv-knowledge-merge/query/index/tier/amend helpers for umbrella scope; lights up /hv-migrate v4's umbrella branch. Split from F18 to keep the data-shape fold bounded. Tagged v4.1 (M06) — v4.0/M01 acceptance excludes umbrella-KNOWLEDGE and forbids partial ship. Detail: `.hv/features/F21.md` Related: [F18], [F19] Milestone: M06 Since: a0e7f1f~~ Done 2026-05-19 [`680961a`]
- ~~**[T103] Reconcile `--repo` flag-parse boilerplate across the 7 knowledge/glossary helpers.** `REPO_FLAG=""; --repo) … shift 2` appears in 3 byte-level variants: `hv-knowledge-merge/tier/amend` (case parser, `${2:?$USAGE}` guard), `hv-knowledge-query` (case parser, bespoke error message, `$HERE`-style source path), and `hv-glossary-write/read/import` (`-*) unknown flag` parser, NO `${2:?}` guard — bare trailing `--repo` silently sets `REPO_FLAG=""`, a real bug). Goal: a sourceable arg-eater or a standardized arg-loop convention shared by both helper families, with one usage-error guard. Acceptance: single-repo output stays byte-identical; smoke asserts bare trailing `--repo` exits with a usage error in all 7 helpers. Related: [F21] Milestone: M06 Since: 3ed1ba5~~ Done 2026-05-19 [`6fdd0f1`]
- ~~**[T104] Unify duplicated umbrella/hybrid parsers in hv-knowledge-query and hv-glossary-read.** Each reader carries the bullet/term parser TWICE — umbrella branch inlines it (hv-knowledge-query L105-139, hv-glossary-read L63-79), hybrid branch wraps the identical algorithm in a function (`collect_bullets()` L196-219, `extract_entries()` L130-154). Goal: collapse to one path by treating umbrella as the degenerate 1-element case of the hybrid (single file pair, suppress `> from:` when scope is umbrella). Hard acceptance: single-repo output BYTE-IDENTICAL pre/post — gate the commit on `diff <(old) <(new)` across multiple topics AND `--tier provisional/confirmed/deprecated` filters. Related: [F21] Milestone: M06 Since: 3ed1ba5~~ Done 2026-05-19 [`6811313`]
