# TODO

## Bugs

## Features

## Tasks

## Completed
- ~~**[B01] [P1] `/hv-capture` Step 7 auto-dispatches `/hv-brainstorm` on `autonomy=auto`, conflating capture with planning.** Capture should be pure intake; auto-invoking brainstorm pulls the user into design exploration mid-brain-dump. Worse, the escalation is inverted: `auto` proactively brainstorms, `loop` skips entirely. Fix: Step 7 prints the nudge line in all modes (off/auto/loop) and never invokes the Skill tool. Move autonomous advancement to `/hv-next` where "advance without asking" semantics belong. Related: [F01], [F03]
- ~~**[F27] [Minor] /hv-capture should audit code state before capturing from stale milestone spec.** When `/hv-capture` is invoked against a milestone spec (`.hv/milestones/M<NN>.md`), it appends items without auditing whether they're already shipped under different IDs — if the milestone spec hasn't been kept in sync with code state, the capture creates duplicates and loop mode can then dispatch `/hv-work` against already-done items. Detail: `.hv/features/F27.md` GH: #11 Since: d6f3d20~~ Done 2026-05-17 [`214783b`]
- ~~**[B07] [P1] hv-migrate reads top-level `version`, /hv-init writes nested `hvSkills.version`.** Documented v3 → v4 upgrade dead-ends at "refusing: .hv/config.json has no 'version' field — run /hv-init" even after /hv-init has run. Workaround: hand-stamp top-level `version` via hv-config-set. Fix: nested-first read with legacy fallback. Detail: `.hv/bugs/B07.md` Related: [B08] Milestone: M01 Since: a613b50~~ Done 2026-05-17 [`3ab5eff`]
- ~~**[B08] [P1] /hv-migrate v4 --apply never bumps the recorded version stamp.** After successful apply, hvSkills.version still reports the pre-migration value; hv-preflight keeps nagging "project at X.Y.Z, plugin at A.B.C — run /hv-init" indefinitely. Functional idempotency holds via the plan-based nothing_to_do branch, but recorded state stays stale. Detail: `.hv/bugs/B08.md` Related: [B07] Milestone: M01 Since: a613b50~~ Done 2026-05-17 [`3ab5eff`]
- ~~**[B09] [P1] v3→v4 upgrade: hv-init leaves orphaned managed blocks; hv-migrate codemod is a blind regex (corrupts mentions + helper substrings).** Three defects: (1) /hv-init upgrade-refresh never strips orphaned v3 `<!-- hv-context-* -->` blocks; (2) hv-migrate rewrite_text() is line-regex with no mention/usage discrimination or managed-block awareness (2a corrupts self-documenting text, 2b substring-matches hv-map-query, 2c rewrites instead of deleting orphan block); (3) umbrella projects have no migration path (F21 deferral). Detail: `.hv/bugs/B09.md` Related: [B07], [B08], [F21] GH: #14 Milestone: M01 Since: 74ca9df~~ Done 2026-05-17 [`3ab5eff`]
- ~~**[F25] [Major] hvlib.py package split — 1077 LoC → 6 modules.** hvlib.py owns 8+ unrelated concerns: section-edit, bullet parsing, frontmatter, milestone/repo registry, atomic IO, managed-block, TOML version handling, map iteration, crossref. 14 PYTHONPATH heredoc callers each implicitly know their subset. Split into hvlib/{io,section,bullet,repos,version,crossref}.py with top-level `from hvlib.* import *` preserving backward compat. Needs proper /hv-brainstorm to validate the wildcard re-export strategy and verify every PYTHONPATH caller. Related: [F24]. Milestone: M01 Since: c84ae3e~~ Done 2026-05-18 [`bdaaecf`]
- ~~**[B10] [P2] `issues.providers.{github,gitlab}` config gate accepts None as enabled.** /hv-capture --from-github / --from-gitlab Step I1 reads `cfg.get('issues',{}).get('providers',{}).get('github')` and only stops when the value is literally `false`. On a project that never set the key (config has no `issues` section at all, returns None), the import proceeds silently. Either the key should default to `true` explicitly (current de-facto behavior, just make it schema-visible) or the gate should require `is True` to enable. /hv-init's EXPECTED schema check doesn't include `issues.providers.*` either, so STALE migration never prompts for it. Subsystem: hv-capture Milestone: M01 Since: 4f17322~~ Done 2026-05-18 [`1ba7897`]
- ~~**[F26] [Major] design/plan/spike/vision CRUD triplet consolidation.** Four near-isomorphic CRUD families (16 helpers total: hv-design-{add,rm,show,list}, hv-plan-*, hv-spike-*, hv-vision-* CRUD subset). Each `-rm` is find-then-unlink; each `-show` is find-then-cat; each `-list` walks dir + emits JSON. Adding a 5th artifact kind = 4 new helpers. Approach options: (a) lightweight `hv-artifact-lib.sh` shared by 16 wrappers, (b) full `hv-artifact <kind> <verb>` dispatcher with SKILL.md callsite sweep, (c) Python `Artifact` class in hvlib. Needs /hv-brainstorm. Out of Phase A refactor due to API-shift implications. Milestone: M01 Since: c84ae3e~~ Done 2026-05-18 [`1485be5`]
- ~~**[B11] [P2] hv-managed-block lost --strip-deprecated mode; smoke section 'hv-managed-block — B09: --strip-deprecated removes orphan v3 blocks' fails on main.** Error: `unknown key '--strip-deprecated' (known: knowledge, decisions)`. Likely regression from `c623d2f refactor: simplify hv-skills surface`. Smoke ran red on main before F26 work — F26 ships unaffected. /hv-init's deprecated-block stripping pipeline is broken until restored. Since: 51c5540~~ Done 2026-05-18 [`6922ab2`]
- ~~**[T03] hv-learn Step 8 auto-split orphans knowledge-tier.json (silent tier/hit loss).** `/hv-learn` Step 8 auto-split (steps 3–7) is silent on the tier sidecar — it specifies only KNOWLEDGE.md heading/bullet moves + `hv-knowledge-index`, but renaming a `## Topic` heading orphans every titled bullet's `.hv/knowledge-tier.json` entry, which is keyed `"<topic>::<title>"`. Detail: `.hv/tasks/T03.md` GH: #13 Since: d6f3d20~~ Done 2026-05-18 [`80fa6fd`]
- ~~**[T04] Orchestrator absorbs wave-internal file collisions for parallel dispatch.** When a multi-task plan has two T# steps that touch the same file and `work.isolation` is `branch`, the parallel-dispatch guard forces them to serialize unless the orchestrator merges one task's edit into the other at dispatch time. Detail: `.hv/tasks/T04.md` GH: #12 Since: d6f3d20~~ Done 2026-05-18 [`ff5f330`]
- ~~**[T102] No writer helper to set a backlog item's `Milestone:` (or other) field after capture.** `/hv-plan` Step 2 and `/hv-capture` Step 4.5 both require tagging an existing BACKLOG.md entry with a milestone, but the only field helper is the read-only `hv-todo-field`. Today this forces hand-editing `.hv/BACKLOG.md`, which the project explicitly forbids ('don't edit .hv/ by hand — use the skill helpers'). Add a `hv-todo-set-field <ID> <field> <value>` writer (mirroring `hv-todo-field`'s field vocabulary: milestone/related/repos/subsystem) and route `/hv-plan`'s tag step and any retag flow through it. Surfaced while planning M06-F21 (F21 had to be milestone-tagged by hand-editing). Milestone: M01 Since: e7dbbf0~~ Done 2026-05-19 [`47294c2`]
- ~~**[F21] [Major] Umbrella-aware KNOWLEDGE.md.** Per-sub-repo .hv/knowledge/<name>/KNOWLEDGE.md storage; rewire hv-knowledge-merge/query/index/tier/amend helpers for umbrella scope; lights up /hv-migrate v4's umbrella branch. Split from F18 to keep the data-shape fold bounded. Tagged v4.1 (M06) — v4.0/M01 acceptance excludes umbrella-KNOWLEDGE and forbids partial ship. Detail: `.hv/features/F21.md` Related: [F18], [F19] Milestone: M06 Since: a0e7f1f~~ Done 2026-05-19 [`680961a`]
- ~~**[T103] Reconcile `--repo` flag-parse boilerplate across the 7 knowledge/glossary helpers.** `REPO_FLAG=""; --repo) … shift 2` appears in 3 byte-level variants: `hv-knowledge-merge/tier/amend` (case parser, `${2:?$USAGE}` guard), `hv-knowledge-query` (case parser, bespoke error message, `$HERE`-style source path), and `hv-glossary-write/read/import` (`-*) unknown flag` parser, NO `${2:?}` guard — bare trailing `--repo` silently sets `REPO_FLAG=""`, a real bug). Goal: a sourceable arg-eater or a standardized arg-loop convention shared by both helper families, with one usage-error guard. Acceptance: single-repo output stays byte-identical; smoke asserts bare trailing `--repo` exits with a usage error in all 7 helpers. Related: [F21] Milestone: M06 Since: 3ed1ba5~~ Done 2026-05-19 [`6fdd0f1`]
- ~~**[T104] Unify duplicated umbrella/hybrid parsers in hv-knowledge-query and hv-glossary-read.** Each reader carries the bullet/term parser TWICE — umbrella branch inlines it (hv-knowledge-query L105-139, hv-glossary-read L63-79), hybrid branch wraps the identical algorithm in a function (`collect_bullets()` L196-219, `extract_entries()` L130-154). Goal: collapse to one path by treating umbrella as the degenerate 1-element case of the hybrid (single file pair, suppress `> from:` when scope is umbrella). Hard acceptance: single-repo output BYTE-IDENTICAL pre/post — gate the commit on `diff <(old) <(new)` across multiple topics AND `--tier provisional/confirmed/deprecated` filters. Related: [F21] Milestone: M06 Since: 3ed1ba5~~ Done 2026-05-19 [`6811313`]
- ~~**[F76] [Minor] `/hv-plan` should validate doc-deliverable paths against the repo/umbrella layout.** A /hv-plan task placed a doc deliverable at `runlog/docs/NN-x.md` but that repo had no `docs/` dir — the mismatch only surfaced mid-/hv-work. /hv-plan should validate doc-by-path deliverables against the target repo's layout before the plan is written, surfacing cross-repo doc mismatches as an open question. Detail: `.hv/features/F76.md` GH: #15 Since: 5ad9273~~ Done 2026-05-20 [`be6fa02`]
- ~~**[F77] [Minor] `/hv-release` should detect and offer to close open upstream GH/GL issues for shipped items.** Mirrors the gate `/hv-ship` Step 6c runs on the direct-merge path. Pushing directly to main skips `/hv-ship` entirely — F76 shipped in v4.3.0 but GH #15 stayed open. Scope: add `--open-only` flag to `bin/hv-issues-imported` (filters by live upstream state via gh/glab), new Step 13.6 in `hv-release/SKILL.md` (manual gate, parallel `hv-issues-close` dispatch using the release commit SHA), update `hv-ship` Step 6c to also use `--open-only`. Add smoke coverage for the helper flag. Related: [F76] Subsystem: release Since: ae8877b~~ Done 2026-05-22 [`bad41d2`]