## What happened
A /hv-plan task placed a doc deliverable at `runlog/docs/NN-x.md`, but that repo has no `docs/` dir — architectural/compliance docs live in a SEPARATE sibling repo (`runlog-docs`). The mismatch only surfaced mid-/hv-work (the doc task couldn't be placed without either fragmenting the doc home or breaking the cycle's single-repo `Repos:` scope), forcing an execution-time replan + user decision.

## Expected
/hv-plan (Step 3 context load / Step 4 task decomposition) should validate that any task whose deliverable is a doc-by-path resolves to an existing doc home in the target repo, and flag cross-repo doc deliverables that don't fit a single-repo-scoped cycle BEFORE the plan is written — surfaced as an open question, not discovered during /hv-work.

## Context
- hv-skills version: 4.1.0
- Captured topic: Architecture: Cross-repo workflow & hv-work orchestration
- Date: 2026-05-19
- Repro context: umbrella workspace (multi-repo, non-git root), item scoped `Repos: runlog`, doc deliverable belonged in sibling `runlog-docs`.

---
**Upstream:** https://github.com/l4ci/hv-skills/issues/15
**Captured from:** github #15
